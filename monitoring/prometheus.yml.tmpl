global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "go-backend"
    static_configs:
      - targets: ["${BACKEND_GO_HOST}:${BACKEND_GO_PORT}"]

  - job_name: "python-backend"
    static_configs:
      - targets: ["${BACKEND_PYTHON_HOST}:${BACKEND_PYTHON_PORT}"]
